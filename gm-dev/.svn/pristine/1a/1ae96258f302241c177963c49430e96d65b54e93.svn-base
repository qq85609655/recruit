<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.csot.gm.materialManage.dao.extension.enumValue.EnumValueExtMapper">

  <resultMap id="EnumValueMap" type="com.csot.gm.materialManage.model.extension.enumValue.EnumValueExt">
    <result column="ID" property="id" jdbcType="VARCHAR" />
    <result column="ENUM_VALUE" property="enumValue" jdbcType="VARCHAR" />
    <result column="ENUM_TEXT" property="enumText" jdbcType="VARCHAR" />
  </resultMap>

  <sql id="Example_Where_Clause">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" suffix=")" prefixOverrides="and">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition}
                  #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition}
                  #{criterion.value} and
                  #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach collection="criterion.value" item="listItem" open="(" close=")"
                    separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>

  <select id="getEnumValueByType" parameterType="map" resultMap="EnumValueMap">
    select gev.id,
    gev.enum_value, gev.enum_text from t_gm_enum_value gev where gev.enum_type_id = #{enumType}
    order by gev.enum_text asc
  </select>

  <delete id="deleteEnumValueByType" parameterType="java.lang.String">
    delete from
    T_GM_ENUM_VALUE where
    ENUM_TYPE_ID = #{enumType,jdbcType=VARCHAR}
  </delete>

  <select id="getByTypeIdAndEnumText" parameterType="map" resultMap="EnumValueMap">
    select
    gev.id,
    gev.enum_value, gev.enum_text from t_gm_enum_value gev where gev.enum_type_id =
    #{enumTypeId} and
    gev.enum_text = #{enumText}
  </select>

  <select id="getByLikeEnumText" parameterType="map" resultMap="EnumValueMap">
    select gev.id,
    gev.enum_value,
    gev.enum_text from t_gm_enum_value gev where upper(gev.enum_text) like
    #{enumText}
  </select>

  <select id="getByLikeEnumTextAndValue" parameterType="map" resultMap="EnumValueMap">
    select
    gev.id,gev.enum_value, gev.enum_text from t_gm_enum_value gev where upper(gev.enum_text) like
    #{enumText} or upper(gev.enum_value) like #{enumText}
  </select>

  <select id="getByTypeIdAndEnumValue" parameterType="map" resultMap="EnumValueMap">
    select
    gev.id,
    gev.enum_value, gev.enum_text from t_gm_enum_value gev where gev.enum_type_id =
    #{enumTypeId} and
    gev.enum_value = #{enumValue}
  </select>

  <select id="getByTypeIdAndEnumValueLike" parameterType="map" resultMap="EnumValueMap">
    select gev.id,
    gev.enum_value, gev.enum_text from t_gm_enum_value gev where
    upper(gev.enum_value) like
    #{enumValue} and gev.enum_type_id = #{enumTypeId}
  </select>
  
  <select id="getByEnumButId" parameterType="map" resultMap="EnumValueMap">
    select gev.id,
    gev.enum_value, gev.enum_text from t_gm_enum_value gev where
    (gev.enum_value = #{enumValue} or gev.enum_text = #{enumText})
    and gev.id != #{enumValueId}
  </select>
  
  <select id="countByExample" parameterType="com.csot.gm.materialManage.model.extension.enumValue.EnumValueExtCriteria"
    resultType="java.lang.Integer">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select count(*) from T_GM_ENUM_VALUE
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
  </select>
  <sql id="Oracle_Dialect_Pagination_Prefix">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <if test="page != null">
      select * from ( select row_.*, rownum rownum_ from (
    </if>
  </sql>
  <sql id="Oracle_Dialect_Pagination_Suffix">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <if test="page != null">
      <![CDATA[ ) row_ ) where rownum_ > #{page.begin} and rownum_ <= #{page.end} ]]>
    </if>
  </sql>
</mapper>